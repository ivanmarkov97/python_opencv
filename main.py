from virtual_paint import main as paint_main
from virtual_paint import default_config as paint_config

from document_scanner import main as scanner_main
from document_scanner import default_config as scanner_config


if __name__ == '__main__':
	# paint_main(paint_config)
	scanner_main(scanner_config)
